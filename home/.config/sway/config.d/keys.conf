# ---------------------------------------------------------------------------
# Purpose: Keybindings for Sway. This file contains `bindsym` blocks and modes
#          (e.g. resize) to map keyboard shortcuts to actions, program launchers
#          and workspace management.
# ---------------------------------------------------------------------------

# Kill focused window
bindsym $mod+Shift+q kill

# Reload the configuration file
bindsym $mod+Shift+r reload

# Exit sway (logs you out of your Wayland session)
bindsym $mod+Shift+p exec swaynag -t warning \
    -m 'You pressed the exit shortcut. Do you really want to exit sway? This will end your Wayland session.' \
    -B 'Yes, exit sway' 'swaymsg exit'

# Programs
bindsym $mod+Return exec $term
bindsym $mod+Space exec ~/bin/rofillo
bindsym $mod+Shift+Space exec ~/bin/bookmarks
bindsym $mod+Control+Space exec ~/bin/shortcuts
bindsym Print exec ~/bin/screenshot

# Move your focus around
bindsym $mod+$left focus left
bindsym $mod+$down focus down
bindsym $mod+$up focus up
bindsym $mod+$right focus right
bindsym $mod+q focus prev
bindsym $mod+Control+q focus parent
bindsym $mod+e focus next
bindsym $mod+Control+e focus parent

# Move the focused window with the same, but add Shift
bindsym $mod+Shift+$left move left
bindsym $mod+Shift+$down move down
bindsym $mod+Shift+$up move up
bindsym $mod+Shift+$right move right

# Workspaces
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+w exec swaymsg workspace number $(~/bin/swayws up)
bindsym $mod+a exec swaymsg workspace number $(~/bin/swayws left)
bindsym $mod+s exec swaymsg workspace number $(~/bin/swayws down)
bindsym $mod+d exec swaymsg workspace number $(~/bin/swayws right)

# Move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+w exec swaymsg move container to workspace number $(~/bin/swayws up)
bindsym $mod+Shift+a exec swaymsg move container to workspace number $(~/bin/swayws left)
bindsym $mod+Shift+s exec swaymsg move container to workspace number $(~/bin/swayws down)
bindsym $mod+Shift+d exec swaymsg move container to workspace number $(~/bin/swayws right)

# Layout stuff
bindsym $mod+b splith
bindsym $mod+v splitv

# Switch the current container between different layout styles
bindsym $mod+z layout toggle split
bindsym $mod+x layout stacking
bindsym $mod+c layout tabbed

# Make the current focus fullscreen
bindsym $mod+f fullscreen

# Toggle the current focus between tiling and floating mode
bindsym $mod+Shift+f floating toggle

# Toggle sticky mode
bindsym $mod+y sticky toggle

# Drag floating windows by holding down $mod and left mouse button.
# Resize them with right mouse button + $mod.
# Despite the name, also works for non-floating windows.
# Change normal to inverse to use left mouse button for resizing and right
# mouse button for dragging.
floating_modifier $mod

mode "resize" {
    bindsym $left resize shrink width 10px
    bindsym $down resize grow height 10px
    bindsym $up resize shrink height 10px
    bindsym $right resize grow width 10px

    bindsym Return mode "default"
    bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# ---------------------------------------------------------------------------
# Purpose: Define reusable variables (mod key, direction keys, terminal, etc.)
# ---------------------------------------------------------------------------

# Variables

# Logo key. Use Mod1 for Alt.
set $mod Mod4

# Home row direction keys, like vim
set $left h
set $down j
set $up k
set $right l

# Your preferred terminal emulator
set $term foot

# Workspaces start
set $ws1 "11:🌐:1"
set $ws2 "21:💻:1"
set $ws3 "31:💬:1"
set $ws4 "41:🍞:1"
# Workspaces end
